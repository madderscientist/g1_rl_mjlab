# 实验流水账

按时间顺序记录尝试过什么、结论是什么。**最终采用的方案见各 README，这里只记过程和被排除的方向。**

写这份东西的目的只有一个：别再走一遍已经走过的死路。

---

## 08-11 ~ 08-14　起点与站立任务

从 `unitree_rl_mjlab`（mjlab 1.2.0）迁移到 mjlab 1.5.x，只保留下肢，重整结构。

| 尝试 | 结论 |
|---|---|
| actor 换 GRU | 采用（下肢行走任务） |
| 手臂重力补偿 + 负载惯量随机化 | 采用，对应真机上把补偿力矩折算成位置偏移 |
| `entropy_coef` 0.01 → 0.002 + σ 上界 | 采用；按网络结构区分，GRU 在跨课程档时压 σ |
| 加重腰关节与躯干侧倾惩罚 | 权重偏轻，未达预期 |
| 最小力矩站立任务（无 command） | 采用 |
| 删掉「末端跟丢即终止」判据 | 采用——过早停止让策略学不到恢复 |

**踩过**：`actuator_force` 是执行器序，不能用 `joint_ids` 索引。

---

## 08-18　复现 Extreme-RGMT

一次性引入两阶段框架：

- **Stage I 架构**：3 分支编码器 → 交错 token 因果自注意力 → 以 $h_t$ 为 Query 对参考窗口做交叉注意力 → FSQ 量化 → MLP 输出残差动作
- **Stage II**：PACE（分层采样 + BC 锚定）+ STAR（按难度重加权）

Stage I 架构留到今天仍是主线。Stage II 后面被反复证伪，见下。

---

## 08-20　三项基础改进（都有效）

| 改进 | 效果 |
|---|---|
| **概率终止**（Stubborn 式，$p=0.005$） | 失败后平均再跑 200 步，恢复行为作为副产物涌现，**不需要任何恢复专用奖励** |
| **参考窗口改非均匀**（近密远疏，±0.3 s） | 按实测注意力分布收窄 |
| **语料按 clip p90 速度重加权** | 纠正语料先天偏斜 |

概率终止用几何分布而非固定延迟是关键：无记忆性让 TD 更新保持一致，固定延迟会在截断处引入价值估计偏差。

**踩过**：三个失败判据必须共享同一次伯努利采样，否则实际终止率翻三倍、窗口缩到 1.3 s。

---

## 08-21　抬脚问题、AMP、第一次移除 Stage II

### 「抬不起脚」的三次归因

长期存在的问题：机器人拖着脚走。归因走了两次弯路才找对。

| 归因 | 验证 | 结论 |
|---|---|---|
| 终止太严，抬脚就判死 | 放宽阈值 | **错**，与终止机制无关 |
| 奖励被稀释 | 调权重 | **也错** |
| **策略主动「削峰」保平衡** | — | **对**。抬脚的收益是跟踪奖励，代价是失稳风险，策略选择了保守 |

解法：`motion_swing_lift_ratio`，按**比值**评分而非绝对高度，参考未要求抬脚时返回 1.0，不干预支撑相（占 73%）。抬脚达参考的比例做到 99.7%。

### 已排除：AMP（对抗式风格奖励）——**两个版本都试过**

动机：跟踪奖励是逐帧 $\exp(-\lVert e\rVert^2/\sigma^2)$，对**系统性**偏差几乎无感——拖着一只脚
（差 0.107 m）摊到 14 个 body 上只让奖励掉 0.9%。判别器看的是状态转移的**分布**，理论上对症。

**v1**（自研）漏了四处：观测归一化、replay buffer、风格奖励用 $-\log(1-D)$ 而非有界的
最小二乘式、logit 正则。后果是判别器学不动：分离度从 `d_expert +0.234 / d_policy −0.357`
萎缩到 `+0.045 / −0.040`，代进 $r = 1 - 0.25(d-1)^2$ 得 **0.730 ≈ 常数**，乘 `amp_coef=0.5`
就是每步无差别 +0.365。

| 参考抬脚 | 起点 | `motion_swing_lift` | AMP v1 |
|---|---|---|---|
| >0.05 m | 0.64× | **0.82× (+29%)** | 0.55× (**−13%**) |
| 存活 | 143.0 s | **162.2 s** | 147.7 s |
| 体位误差 | 0.075 | **0.074** | **0.082** |

**v2** 对齐官方 `nv-tlabs/ASE` 重写，四处全补。判别器确实活了（不再塌到 0.5，两组 eplen ~775
健康），**但跟踪质量仍无收益**。

结论：AMP 是风格先验，对「跟得准」不对症。代码保留在 `g1_lower_rl/rl/amp.py`（`amp_coef=0`
默认关闭，对原训练零影响）。

> **两条教训**：
> 1. 辅助奖励只要输出趋于常数，就退化成生存奖励并稀释主任务信号。上线判别器类奖励必须
>    监控它的**判别能力本身**（`acc_expert` / `acc_agent` 塌到 0.5 就是已死）。
> 2. **实现修对了，方向错了还是没用。** v1 的失败很容易归因成 bug，修好才能确认方向本身
>    不对——否则会一直惦记着「是不是我写错了」。

### 第一次移除 PACE/STAR

当时用弱模型分层，challenging 里 94% 是「五次 rollout 一次都没成功」的片段，没有最近发展区。
（后来因为翻跟头需求又恢复了一次，见 08-26，第二次证伪更彻底。）

---

## 08-22 ~ 08-24　参考 token 扩容与防漂移

### 有效：参考 token 补 key body 信息

参考 token 从 38 维扩到 68 维，追加 5 个关键 body 的位置和线速度，**表达在机器人当前 anchor 的 yaw 局部系下**（不是参考系）。

> 这一点是部署时最容易搞错的地方：用参考位姿算等于退回开环。

### 防漂移的分离实验

上游 `anchor_pos` 判据是 `bad_anchor_pos_z_only`——**只看高度，漂 5 米也不终止**，于是「漂着但站得好好的」永远不被判负，反因存活久被 advantage 强化。

换成全 3D 版本后 250 s 漂移从 5.71 m 压到 0.41 m（**−93%**）。分离实验（只改奖励 vs 也收终止阈值）表明：

- **−76 个百分点来自奖励本身**
- 只有最后 17 个点来自收紧终止，而这 17 个点要用 9 s 存活 + 手部与全身精度去换

所以最终取 `ANTI_DRIFT=5`：**要奖励，放宽终止到 2.0 m**。

### 消融：收益不是「训练久了」带来的

从头训练对照（同迭代数）：

| 迭代 | 38 维参考 token | 68 维参考 token |
|---|---|---|
| 10000 | 5.5 | 5.3 |
| 20000 | **6.0** | 5.2 |
| 30000 | 5.3 | **6.0** |
| 40000 | 5.7 | **10.1（1.77×）** |

⚠️ **前 20000 迭代旧配置一路领先**（多 15 维输入在早期是学习负担）。**若在 20000 处停手，
会得出「改动有害」的完全相反结论。** 结构性改动的消融终点必须设在两条曲线都走平之后。

> 证据边界：消融只跑到 40000，交付模型是 215787，**只覆盖 19%**，且 n=1 单种子。
> 能说的是「相同迭代数下新配置更好且差距扩大」，不能说「最终水平更高」。

### 已排除：放宽终止阈值、腿部力矩不够

两个自然猜想都被实测推翻：

- **不是阈值问题**：`anchor_pos` + `ee_body_pos` 从 0.25 放宽到 1.0 m，存活仅 20.9 → 26.7 s，
  且**全部 361 次终止改判为 `anchor_ori`（真倾倒）**。机器人是真在摔。
- **腿也不是硬件受限**：摔前 1 s 腿部力矩峰值饱和度均值仅 0.69。决定性反证是
  **死得最快的类别力矩余量反而最大**（sprint1 0.56，而活得久的 run1 是 0.90）。

> 别把**阈值**和**终止机制**搞混：前者决定「多大误差算失败」，放宽它只是晚点被判死（+28%）；
> 后者决定「判死后是否继续跑」，改成概率终止才让恢复行为进入 rollout（+67%）。

---

## 08-25 ~ 08-26　翻跟头：数据、Stage II 复活

导入 Extreme-RGMT 的高动态数据（HuggingFace `elijahgalahad/any4hdmi-g1-extreme-motions`），5 clips + 镜像 = 10 条，**总共只有 42 秒**，倒置帧占 2.7~9.1%。

viser 逐帧看下来：机器人**不会翻**，只是在地上苟活。于是恢复 Stage II 再验一次。

---

## 08-26 ~ 08-27　Stage II 的三级排除法

这是这段时间花费最多、结论最硬的一次。**翻转完成度**（参考倒置时机器人也倒置的帧占比）作为主指标。

| 假设 | 做法 | 结果 |
|---|---|---|
| challenging 层里翻跟头只占 0.67%，太稀 | 按动力学分数聚焦到 6.7% | 0.000 → 0.013，可忽略 |
| STAR 式(20) 的 $B$ 实现错了 | 修正 | 0.013 → **0.000**，没帮助 |
| **PACE 的 BC 锚定** | — | **就是它** |

### 根因

式(15) 的 $L_{con} = \mathbb{E}\left[\lVert a_\theta(s) - a_{ref}(s)\rVert^2\right]$ 把策略往 base policy 上拉。

- 论文的 base policy 有 **21.42%** 高动态能力
- 我们的只有 **3.6%**

BC 锚住的正是「不会翻」。旁证：Stage II 的误差**更低**（倒置帧 0.566 vs 0.598），因为它在优化「更贴近那个不翻的参考」——**误差越低，翻得越死**。

决定性对比：纯 PPO 给翻跟头的采样份额只有 **0.05%**，完成度 0.091；Stage II 给到 **6.7%**（134 倍），完成度 **0.000**。

### 顺带发现的两个真 bug

1. **式(20) 的 $B$**：应该是「参与竞争的 bin 数」，不是全局 bin 数。用 `numel()` 会让 $w$ 均值变成 84380/1236 ≈ 68，$w>1$ 恒真，高难组退化成 acquisition 组。诊断指纹是 `star_high_ratio` 恰好等于 $\xi=0.8$。
2. **式(17) 的 $\rho$**：论文用有效样本比例，前提是**失败即终止**。本仓用概率终止，`dones` 太稀疏，算出来的 $\rho$ 恒等于环境分配比 0.8，自适应形同虚设。改读环境侧的失败态 EMA。

---

## 08-27　测量噪声：一次必要的自我纠正

同一个权重测六次翻转完成度：0.112 / 0.099 / 0.112 / 0.033 / 0.050 / 0.083——**3.4 倍方差**，因为只有 455 个翻转帧。

提高到 3903 帧（`--envs-per-motion 24`）后：

| 权重 | 低样本量 | 高样本量 |
|---|---|---|
| 起点 | 0.007 | **0.036** |
| 纯 PPO | 0.112 | **0.091** |

**真实增益是 2.5 倍，不是我先前说的 16 倍。** Stage II 的 0.000 在 3709 帧下复现，那个不是噪声。

> **铁律**：翻转完成度必须用 `--envs-per-motion 24`，低样本量的结论一律不可信。

---

## 08-28　$\lambda=0$：最后一次给 Stage II 机会

假设：既然 BC 是元凶，那**去掉 BC、只留分层采样**总该有用吧？

双卡跑 12000 迭代，验证 `lambda_effective = 0.0000`、`con_over_ppo = 0.0000`（BC 完全关闭）。同一批 103561 配对帧、3405 翻转帧内比较：

| 模型 | 翻转完成度 | 倒置帧误差 | 250 s 基础跟踪 |
|---|---|---|---|
| 纯 PPO | **0.046** | 0.5915 | **165.6 s** |
| λ=0 宽分层 | 0.035 | 0.5714 | 147.2 s |
| λ=0 聚焦分层 | **0.022** | 0.5770 | **114.2 s** |

**采样越聚焦，翻转反而越差**，基础跟踪掉 11%~31%。

结论：**Stage II 整条路彻底排除**——不只 BC 有害，去掉 BC 只留分层采样同样有害。42 秒的高动态数据就是天花板，采样策略救不了数据量。

---

## 已排除方向汇总（别再试）

| 方向 | 为什么不行 |
|---|---|
| PACE / STAR（Stage II 全套） | BC 把「不会翻」锚死；λ=0 去掉 BC 后分层采样仍然有害 |
| AMP 对抗式风格奖励 | v1 实现有缺陷，v2 对齐 ASE 后判别器健康但**仍无收益**；风格先验对「跟得准」不对症 |
| 拆分「跟丢」与「倒地」终止 | 净负面 |
| 放宽终止**阈值**来解决抬脚 | 与终止机制无关，真因是策略主动削峰 |
| 归因抬脚问题到奖励稀释 | 也错 |
| 收紧 3D 终止阈值来防漂移 | 93% 的收益里只有 17 个点来自它，却要赔上 9 s 存活和精度 |
| 「手臂跟不上是硬件瓶颈」 | **已被五项实测推翻**，真实形态是误差随参考手速单调上升的相位滞后 |

---

## 方法论层面的教训

这些错误我犯过不止一次，单独列出来。

1. **低样本量的行为指标不可信**。同一权重 3.4 倍方差，差点让我把噪声当成 16 倍提升写进结论。
2. **`timeout N bash -c 'while ...'` 必须检查返回值**。不检查就 `echo "done"`，两次谎报「训练提前退出」。
3. **`pgrep -cf "pattern"` 会数到自己**。判活不要用它。
4. **ruff 数量往任何方向偏离基线都是警报**。有次从 73 掉到 69，不是代码变干净了，是语法被我改坏、整个文件没被解析。
5. **函数中段的 `return` 早退会跳过后面所有配置**。给 GRU 分支写早退，跳过了传感器注册和概率终止——即使不崩，跑出来的也是变量全乱的对照组。
6. **训练期的 `eplen` / `reward` 只能用来发现崩溃，不能判断质量**。跨策略比较必须按 `(motion_id, frame)` 配对。
7. **从 git 历史恢复的代码需要重新对齐接口**，不能直接跑。
8. **自动化脚本里的检查点必须能拦截**。我写了「启动后 3 分钟抓日志」，抓到空值却只是记录下来继续走，结果 GPU 空转了 9.8 小时。
